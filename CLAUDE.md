# دليل المشروع (CLAUDE.md)

هذا الملف هو "System Prompt" مرجعي يُستخدم في مرحلتين: **التخطيط** قبل أي عمل، و**القواعد الصارمة** أثناء الكتابة الفعلية للكود. يجب اتباعه حرفياً في كل مهمة داخل هذا المستودع.

---

## 1. برومبت التخطيط (Planning Prompt)

قبل كتابة أي سطر كود، يجب المرور بالخطوات التالية بالترتيب:

1. **فهم الطلب**: أعد صياغة المطلوب بجملة واحدة واضحة. إذا كان هناك غموض حقيقي (قرار لا يمكن استنتاجه من السياق)، اسأل المستخدم قبل المتابعة. لا تخمّن قرارات معمارية مصيرية.
2. **استكشاف السياق الحالي**: افحص البنية الحالية للمشروع (الملفات، الاعتماديات، الأنماط المتبعة) قبل اقتراح أي تغيير. لا تفترض بنية غير موجودة.
3. **تحديد أصغر تغيير كافٍ**: حدد أقل تعديل ممكن يحقق الهدف دون كسر أي شيء آخر. ارفض أي "تحسين" غير مطلوب.
4. **تحديد الملفات المتأثرة**: اذكر بدقة أي الملفات ستُنشأ/تُعدَّل/تُحذف، ولماذا.
5. **خطة التنفيذ**: قسّم العمل إلى خطوات صغيرة قابلة للتحقق، كل خطوة يجب أن تترك المشروع في حالة تعمل (لا حالات وسيطة معطوبة).
6. **خطة التحقق**: حدد كيف سيتم التأكد أن التغيير يعمل فعلاً (اختبار، تشغيل فعلي، فحص يدوي) قبل اعتباره منتهياً.
7. **تقييم الخطورة**: إن كانت الخطوة لا رجعة فيها (حذف، force-push، تعديل بنية قاعدة بيانات، نشر) — أوقف ونبّه المستخدم صراحة قبل التنفيذ.

---

## 2. القواعد الصارمة للبرمجة (Strict Coding Rules)

### أ. النطاق والبساطة
- لا تُضف ميزات أو تجريدات (abstractions) لم يُطلب منك إضافتها.
- لا تصمم لحالات مستقبلية افتراضية. ثلاثة أسطر متشابهة أفضل من تجريد سابق لأوانه.
- لا تترك تطبيقات ناقصة (half-finished) — إما أن تُنجز الميزة كاملة أو لا تبدأها.
- إصلاح خطأ لا يستدعي إعادة هيكلة الكود المحيط به.

### ب. الأمان
- امنع الثغرات الشائعة (OWASP Top 10): حقن الأوامر، XSS، حقن SQL، إلخ.
- لا تثق بأي مُدخل قادم من المستخدم أو مصدر خارجي دون تحقق عند حدود النظام.
- لا تُخزّن أسراراً (مفاتيح، توكنات، كلمات مرور) في الكود أو في الالتزامات (commits).

### ج. معالجة الأخطاء
- لا تُضف معالجة أخطاء أو fallback لحالات لا يمكن أن تحدث فعلياً.
- تحقق فقط عند حدود النظام (مدخلات المستخدم، استدعاءات API خارجية).
- ثق بضمانات الكود الداخلي وإطار العمل المستخدم.

### د. التعليقات والتسمية
- الافتراضي: **بدون تعليقات**. أسماء المتغيرات والدوال الجيدة تُغني عن الشرح.
- أضف تعليقاً فقط عندما يكون "لماذا" غير واضح (قيد خفي، حل التفافي لخطأ معروف، سلوك مفاجئ).
- لا تكتب تعليقات تشرح "ماذا يفعل" الكود، ولا تُشير إلى المهمة الحالية أو رقم Issue.
- لا تُبقِ على أكواد قديمة معلّقة (commented-out) أو ملاحظات "// تم الحذف".

### هـ. لا حِيَل توافقية زائفة
- لا تُبقِ متغيرات غير مستخدمة بأسماء معدّلة (`_var`) لمجرد "التوافق الخلفي" دون داعٍ فعلي.
- إن كان شيء غير مستخدم فعلاً بشكل مؤكد، احذفه بالكامل بدل تعطيله.

### و. Git والعمليات الخطرة
- لا تُنفّذ عمليات لا رجعة فيها (`git reset --hard`, `git push --force`, حذف فروع, `rm -rf`) دون تأكيد صريح من المستخدم لهذه العملية بالتحديد.
- قبل أي أمر قد يُفقد تعديلات غير محفوظة، شغّل `git status` أولاً واحفظ العمل (`stash` أو commit).
- التزامات (commits) صغيرة، رسائل واضحة تشرح "لماذا" لا "ماذا".
- لا تتخطَّ الـ hooks (`--no-verify`) إلا بطلب صريح من المستخدم.

### ز. الاختبار والتحقق
- أي تغيير في الكود يجب التحقق من عمله فعلياً (تشغيل، اختبار، أو فحص يدوي) قبل اعتباره مكتملاً.
- فحص الأنواع (type-check) ونجاح الاختبارات لا يعني أن الميزة تعمل فعلياً من منظور المستخدم — تحقق من المسار الفعلي أيضاً عند الإمكان.

### ح. التواصل
- كن مختصراً ومباشراً في الشرح خارج الكود.
- عند اكتمال مهمة: جملة أو جملتان تلخصان ما تغيّر وما التالي، لا أكثر.
